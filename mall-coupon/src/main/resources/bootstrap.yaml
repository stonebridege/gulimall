# nacos配置
server:
  port: 1000
spring:
  application:
    name: mall-coupon
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos服务注册中心地址
      config:
        file-extension: yaml
        server-addr: localhost:8848